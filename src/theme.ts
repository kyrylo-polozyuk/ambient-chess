/**
 * Programmatically defined colors for use in React style objects and TS/JS.
 * For CSS-only colors, use variables.css.
 */

/** Chessboard square highlight for the last played move (from/to). */
export const chessLastMoveHighlight = "rgba(155, 199, 0, 0.35)"

/** Chessboard square highlight when a piece is selected. */
export const chessSelectedSquareHighlight = "rgba(255, 255, 255, 0.2)"

/** Chessboard square highlight for legal move destinations. */
export const chessLegalMoveHighlight = "rgba(255, 255, 255, 0.15)"
